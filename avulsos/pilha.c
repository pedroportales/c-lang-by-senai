#include <stdio.h>
#include <stdio.h>

typedef struct pilha
{
    int dado;
    struct pilha *prox
} celula;

void imprimir();
void push(int);
void pop();

celula *top = NULL;

int main(int argc, char const *argv[])
{
    int n, opcao;
    return 0;
}

